import { Account } from '../domain/account';

// Stands in for the backend: the only account that exists before someone signs up.
export const MOCK_ACCOUNT: Account = {
  email: 'carlos.restrepo@correo.com',
  password: 'casa-restrepo',
};
